//Use gets puts in string program
#include<stdio.h>                                
void main(){
        char arr[10];
        printf("enter string:\n");
        gets(arr);
        puts(arr);
}
//output with warning for gets() function ignore it for now we will find solution on that also in next programs
